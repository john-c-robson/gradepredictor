package com.example.mobile_dev_assignment;

//Only Use is to allow for 2 variables to be passed between 2 separate Classes.
public interface SelectListener {
    void onItemClicked(int id, int value);
}
